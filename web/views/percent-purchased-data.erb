<%=
hash = Hash.new
@items.each  do |item|
  hash[item.upc] = Hash.new
  method = (@data_shown + "_helper").to_sym
  info = self.send(method, item)
  hash[item.upc]['id'] = item.id
  hash[item.upc]['purchased'] = info[0]
  hash[item.upc]['not purchased'] = info[1]
end
hash.to_json
%>